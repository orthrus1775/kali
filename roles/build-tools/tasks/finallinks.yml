---
# - name: "Creating a windapsearch symlink"
#   ansible.builtin.shell:
#     cmd: ln -s /opt/windapsearch/windapsearch.py /usr/bin/windapsearch
#   become_method: sudo  

# - name: "Creating a hoaxshell symlink"
#   ansible.builtin.shell:
#     cmd: ln -s /opt/hoaxshell/hoaxshell.py /usr/bin/hoaxshell
#   become_method: sudo  

# - name: "Creating a adidnsdump symlink"
#   ansible.builtin.shell:
#     cmd: ln -s "{{ ansible_facts['env']['HOME'] }}/.local/pipx/venvs/adidnsdump/bin/adidnsdump" /usr/bin/adidnsdump
#   become_method: sudo  

- name: "Creating symlinks"
  ansible.builtin.file:
    src: "{{ item.src }}"
    path: "{{ item.dst }}"
    state: link
  loop:
    # - { src: "{{ ansible_facts['env']['HOME'] }}/.local/share/pipx/venvs/adidnsdump/bin/adidnsdump", dst: /usr/bin/adidnsdump }
    # - { src: "{{ ansible_facts['env']['HOME'] }}/.local/share/pipx/venvs/bloodyad/bin/bloodyAD", dst: /usr/bin/bloodyAD }
    # - { src: "{{ ansible_facts['env']['HOME'] }}/.local/share/pipx/venvs/wesng/bin/wes", dst: /usr/bin/wes } 
    - { src: "{{ ansible_facts['env']['HOME'] }}/go/bin/garble", dst: /usr/bin/garble }
    - { src: "{{ ansible_facts['env']['HOME'] }}/go/bin/kerbrute", dst: /usr/bin/kerbrute }
  become: true
  become_method: sudo
  ignore_errors: true 

- name: "Recursively change ownership and permissions of /opt/"
  ansible.builtin.command:
    cmd: "chown -R {{ ansible_facts['user_id'] }}:{{ ansible_facts['user_id'] }} /opt/" 
  become: true
  become_method: sudo
  
  
  
  
  
  
  
  
  
